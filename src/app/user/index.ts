import { types } from "./types";//type of data
import { queries } from "./queries";//type of queries
import { resolvers } from "./resolver";//type of query resolver
import { mutations } from "./mutations";

export const User = {types,queries,resolvers,mutations}